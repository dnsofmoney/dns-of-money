// Re-export the hook so imports read `from "../xaman/useXaman"` — matches
// the hook-file convention and keeps XamanProvider.tsx focused on the provider.
export { useXaman } from "./XamanProvider";
